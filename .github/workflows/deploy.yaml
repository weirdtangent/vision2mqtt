name: Release and Docker Deploy

on:
  push:
    branches:
      - "main"
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 3 1 * *"  # Monthly rebuild at 03:00 UTC on the 1st

permissions:
  contents: write
  issues: write
  pull-requests: write
  packages: write
  security-events: write
  id-token: write  # for cosign signing

jobs:
  lint:
    name: Lint (ruff/mypy)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12', '3.13', '3.14']
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          version: "latest"

      # `uv sync` below re-resolves silently, so a uv.lock that has drifted from
      # pyproject.toml would never surface. Fail loudly instead.
      - name: Lockfile up to date
        if: matrix.python-version == '3.12'
        run: uv lock --check

      - name: Install project (dev)
        run: uv sync --all-extras --dev

      - name: Ruff
        run: uv run ruff check .

      - name: Format (ruff)
        run: |
          uv run ruff --version
          uv run ruff format --check --diff .

      - name: Mypy
        run: |
          uv run mypy src/

      - name: Pip-audit
        if: matrix.python-version == '3.12'
        # Ignored advisories — review each time the lockfile changes substantially (e.g. torch bump):
        #   pygments/requests/pip: no fix available upstream yet
        #   torch PYSEC-2025-189..197, 210, PYSEC-2026-139: OSV false positives;
        #     all 11 advisories list torch 2.9.1 as their highest affected version,
        #     but malformed OSV range events (introduced=0 with empty fixed event)
        #     cause pip-audit to flag torch 2.10.0. Remove if torch is downgraded
        #     below 2.10.0 or if OSV publishes corrected range data.
        #   CVE-2025-3000 (GHSA-rrmf-rvhw-rf47): torch.jit.script memory
        #     corruption, no upstream fix yet (vuln <=2.12.0, patched=null).
        #     Re-evaluate when PyTorch publishes a patched release.
        run: |
          uv run pip-audit \
            --ignore-vuln CVE-2026-4539 \
            --ignore-vuln CVE-2026-25645 \
            --ignore-vuln CVE-2026-3219 \
            --ignore-vuln CVE-2025-3000 \
            --ignore-vuln PYSEC-2026-196 \
            --ignore-vuln PYSEC-2025-189 \
            --ignore-vuln PYSEC-2025-190 \
            --ignore-vuln PYSEC-2025-191 \
            --ignore-vuln PYSEC-2025-192 \
            --ignore-vuln PYSEC-2025-193 \
            --ignore-vuln PYSEC-2025-194 \
            --ignore-vuln PYSEC-2025-195 \
            --ignore-vuln PYSEC-2025-196 \
            --ignore-vuln PYSEC-2025-197 \
            --ignore-vuln PYSEC-2025-210 \
            --ignore-vuln PYSEC-2026-139

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [lint]
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12', '3.13', '3.14']
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          version: "latest"

      - name: Install project (dev)
        run: uv sync --all-extras --dev

      - name: Run tests
        run: uv run pytest tests/ -v

  release:
    name: Semantic Release
    runs-on: ubuntu-latest
    needs: [lint, test]
    if: github.event_name != 'pull_request'
    outputs:
      published: ${{ steps.semrel.outputs.new_release_published }}
      version: ${{ steps.semrel.outputs.new_release_version }}
      tag: ${{ steps.semrel.outputs.new_release_git_tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v6
        with:
          node-version: 20

      - name: Run semantic-release
        id: semrel
        uses: cycjimmy/semantic-release-action@b12c8f6015dc215fe37bc154d4ad456dd3833c90 # v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker:
    name: Build and Push Docker Image
    needs: [release]
    if: needs.release.result == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6

      - name: Fetch tags
        run: git fetch --tags --force

      # metadata-action evaluates its tag list when IT runs, so the version has to
      # be resolved before it. semantic-release only emits a version when it really
      # publishes one — a chore(deps) merge or the monthly rebuild publishes nothing —
      # so fall back to the newest tag. Leaving it empty drops the version tag and
      # pushes a :latest that matches no release.
      - name: Resolve image version
        run: |
          VERSION="${{ needs.release.outputs.version }}"
          if [ -z "$VERSION" ]; then
            VERSION=$(git tag --list 'v[0-9]*' --sort=-v:refname | head -n 1)
            if [ -z "$VERSION" ]; then
              echo "No release and no tags — defaulting to 0.0.0"
              VERSION="0.0.0"
            else
              echo "No new release; reusing newest tag: $VERSION"
            fi
          fi
          # Image tags and SETUPTOOLS_SCM_PRETEND_VERSION both want PEP 440, so drop
          # the leading v that git tags carry.
          VERSION="${VERSION#v}"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "Image version: $VERSION"

      - name: Login to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PAT }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v4.3.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
        with:
          driver-opts: image=moby/buildkit:v0.29.0

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: graystorm/vision2mqtt
          tags: |
            type=raw,value=${{ env.VERSION }}
            type=raw,value=latest
          labels: |
            org.opencontainers.image.title=vision2mqtt
            org.opencontainers.image.description=YOLO object detection service for MQTT camera events
            org.opencontainers.image.url=https://www.graystorm.com
            org.opencontainers.image.documentation=https://github.com/${{ github.repository }}#readme
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Build and push
        id: build-and-push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v6
        with:
          context: .
          pull: true
          push: true
          build-args: |
            VERSION=${{ env.VERSION }}
            ENABLE_NPU=true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/arm64,linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          sbom: true
          provenance: true

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Sign the Docker image
        env:
          DIGEST: ${{ steps.build-and-push.outputs.digest }}
        run: |
          cosign sign --yes graystorm/vision2mqtt@${DIGEST}

      - name: Run Trivy vulnerability scanner
        id: trivy
        continue-on-error: true
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: graystorm/vision2mqtt@${{ steps.build-and-push.outputs.digest }}
          format: sarif
          output: trivy-results.sarif
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true
          exit-code: '0'  # findings must never fail the step; only scanner/infra errors

      - name: Run Trivy vulnerability scanner (retry)
        id: trivy-retry
        if: steps.trivy.outcome == 'failure'
        continue-on-error: true
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: graystorm/vision2mqtt@${{ steps.build-and-push.outputs.digest }}
          format: sarif
          output: trivy-results.sarif
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true
          exit-code: '0'  # findings must never fail the step; only scanner/infra errors

      - name: Warn if Trivy scan failed on both attempts
        if: steps.trivy.outcome == 'failure' && steps.trivy-retry.outcome != 'success'
        run: echo "::warning::Trivy scan failed on both attempts (usually a transient download failure); no SARIF uploaded for this build."

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@27fcff4ecb39e96348e7ceddcc2d9ef42308b6fc # v4
        if: always() && (steps.trivy.outcome == 'success' || steps.trivy-retry.outcome == 'success')
          && hashFiles('trivy-results.sarif') != ''
        with:
          sarif_file: trivy-results.sarif
